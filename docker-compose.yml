services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: plfog
      POSTGRES_USER: plfog
      POSTGRES_PASSWORD: plfog
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
